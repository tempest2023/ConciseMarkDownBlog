# Server-only: never prefix these with REACT_APP_.
PERSONAL_AGENT_ENABLED=false
AI_GATEWAY_API_KEY=
AGENT_MODEL=deepseek/deepseek-v4.1-flash-beta
# Vercel lists this model as experimental, expiring September 10, 2026.
# Recheck availability before deployment; change AGENT_MODEL when replacing it.
# Add a budget to the dedicated Gateway key before enabling public chat.

# Server-only: never prefix these with REACT_APP_.
PERSONAL_AGENT_ENABLED=false
AI_GATEWAY_API_KEY=
# Ordered, comma-separated: primary model first, then Gateway fallback models.
AGENT_MODEL=inception/mercury-2.5,alibaba/qwen3.8-flash
# Free-credit eligibility and provider rate limits can change; recheck before deployment.
# Add a budget to the dedicated Gateway key before enabling public chat.
